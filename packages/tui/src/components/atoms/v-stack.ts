// Public name alias for the layout primitive. Re-exported from the real class
// so `new VStack(...)` / `instanceof VStack` keep working without a duplicate
// behaviourless class body.
export { VStackComponent as VStack } from '../../layout.js';
