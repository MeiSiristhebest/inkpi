// Public name alias for the layout primitive. Re-exported from the real class
// so `new HStack(...)` / `instanceof HStack` keep working without a duplicate
// behaviourless class body.
export { HStackComponent as HStack } from '../../layout.js';
