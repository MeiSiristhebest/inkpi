// Public name alias for the layout primitive. Re-exported from the real class
// so `new Spacer(...)` / `instanceof Spacer` keep working without a duplicate
// behaviourless class body.
export { SpacerComponent as Spacer } from '../../layout.js';
